en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please review the problems below:"
    labels:
      family:
        username: Family Username
        name: Family Name
      user:
        password_confirmation: Confirm Password
    hints:
      family:
        new:
          code: "This is the code chosen in settings."
        edit:
          name: "Your family name, usually your last name. But have fun with this!"
          username: "This code will be required to access the kid's check in and out screen."
          code: "This code will be required to access the kid's check in and out screen."
          token_value: "The amount, in dollars and cents, that each token is worth when converting to money. We use $.50"
        name: "Usually your last name"

    # Examples
    # labels:
    #   defaults:
    #     password: 'Password'
    #   user:
    #     new:
    #       email: 'E-mail to sign in.'
    #     edit:
    #       email: 'E-mail.'
    # hints:
    #   defaults:
    #     username: 'User name to sign in.'
    #     password: 'No special characters, please.'
    # include_blanks:
    #   defaults:
    #     age: 'Rather not say'
    # prompts:
    #   defaults:
    #     age: 'Select your age'
